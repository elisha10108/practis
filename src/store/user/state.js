// to get the info from here to the another components you need to do that like that
//  users = useSelector(state => state.users.     here you need to choose what you need users or error    )
//
export let initialState = {
    users: [],
    error:[]
}